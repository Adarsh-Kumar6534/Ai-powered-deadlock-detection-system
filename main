import sys
from PyQt6.QtWidgets import QApplication
from deadlock_detection.ui import DeadlockDetectionUI

if __name__ == "__main__":
    app = QApplication(sys.argv)
    window = DeadlockDetectionUI()
    window.show()
    sys.exit(app.exec())
